name: Lint

on:
  push:
    branches: [main]
  pull_request:

jobs:
  addon-lint:
    name: Home Assistant add-on linter
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Lint the RitiBackup add-on
        uses: frenck/action-addon-linter@v2
        with:
          path: "./ritibackup"
