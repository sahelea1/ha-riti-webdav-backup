# Runtime dependencies for RitiBackup.
#
# In the addon Docker image these are installed from Alpine's prebuilt apk
# packages (py3-cryptography, py3-aiohttp) for fast, reliable multi-arch builds.
# This file documents the Python-level requirements for running the app or the
# standalone decrypt tool outside the addon (e.g. on a recovery workstation).
cryptography>=41.0
aiohttp>=3.9

# Storage backends. In the addon image these are installed with pip on top of
# the apk packages (see Dockerfile). Listed here for running the app outside the
# addon.
boto3>=1.34      # S3 and S3-compatible object storage
b2sdk>=1.24      # Backblaze B2
